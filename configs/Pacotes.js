class Pacotes {
    express = require("express");
    uid = require("uuid");
    cors = require("cors")
    bodyparser = require("body-parser")
    path = require("path")
}

module.exports = {
    Pacotes
};
